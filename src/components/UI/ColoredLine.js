const ColoredLine = ({ color }) => (
    <hr
        style={{
            color: '#CECECE',
            backgroundColor: '#CECECE',
            height: 0.25
        }}
    />
);

export default ColoredLine;